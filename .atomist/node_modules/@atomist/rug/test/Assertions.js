"use strict";
var Core_1 = require("./Core");
function that(b, msg) {
    return new Core_1.Result(b, msg);
}
exports.that = that;
function equal(actual, expected, msg) {
    if (actual == expected)
        return Core_1.Result.Success;
    return Core_1.Result.Failure(msg ?
        msg :
        actual + " was not equal to " + expected);
}
exports.equal = equal;
